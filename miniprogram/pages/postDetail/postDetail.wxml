<!-- 左上角标题 -->
<van-nav-bar border="{{false}}" fixed left-text="详情" left-arrow bind:click-left="back"/>

<!-- 内容 -->
<view class="main">
	<post-display id="postDisplay" isSingle="{{true}}" post="{{post}}" commentList="{{commentList}}" userInfo="{{userInfo}}"></post-display>
</view>